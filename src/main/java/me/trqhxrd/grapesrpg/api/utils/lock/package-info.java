/**
 * This package contains some locks.
 * Locks can be locked or unlocked.
 * YOu can lock a lock without a key but if you want to unlock it you need a key, which is defined while the locks constructor gets called.
 */
package me.trqhxrd.grapesrpg.api.utils.lock;